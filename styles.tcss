Header Icon {
    display: block;
    width: auto;
}

Header Icon:after {
    content: " command palette : ";
    color: $text-disabled;
    font-style: italic;
    padding-left: 1;
}

Header Icon:hover {
    background: $accent;
    color: $text;
}

#logo {
    text-align: center;
    width: 100%;
    margin-top: 1;
}
#main-scroll {

Grid {
    grid-size: 2 2;
    grid-gutter: 1;
    padding: 1;
}

.panel-header {
    text-align: center;
    background: $primary-darken-2;
    color: $text;
    margin-bottom: 1;
}

#logo {
    text-align: center;
    width: 100%;
}

.hidden {
    display: none;
}

#dashboard-grid.flow-layout {
    grid-size: 1 3;
    grid-rows: auto 1fr auto;
}

#dashboard-grid.flow-layout #todo {
    display: none;
}

#dashboard-grid.flow-layout #clock {
    grid-row-span: 1;
    grid-column-span: 2;
}

#dashboard-grid.flow-layout #pomodoro {
    grid-row-span: 1;
    grid-column-span: 2;
}

#dashboard-grid.flow-layout #info {
    grid-row-span: 1;
    grid-column-span: 2;
}

#flow-intention {
    text-align: center;
    font-size: 150%;
    color: $cyan;
    margin-top: 1;
    margin-bottom: 1;
}

#focus-buddy {
    text-align: center;
    color: $yellow;
    height: 10;
    width: 100%;
}

.state-deep #main-scroll {
    background: $background-darken-1;
}

.state-deep {
    background: $background-darken-1;
}

.modal-panel {
    background: $surface;
    padding: 1 2;
    border: thick $primary;
    width: 60;
    height: auto;
    max-height: 20;
}

#todo-list {
    height: 1fr;
}

.help-text {
    text-align: center;
    color: $text-disabled;
}

#clock, #pomodoro, #info, #todo {
    border: round $primary;
    padding: 1;
    opacity: 0.8;
}

#clock { color: $cyan; }
#info { color: $yellow; }
#pomodoro { color: $green; }

#todo:focus-within, #pomodoro:focus-within {
    border: double $accent;
    opacity: 1.0;
}

#timer {
    color: $warning;
    text-style: bold;
    text-align: center;
}
